# --- Exceeds GitHub's hard 100 MB per-file limit -------------------------
# Features/Features.pkl is 1.0 GB (pre-extracted FaceForensics++ features).
# It cannot be pushed to GitHub without paid Git LFS. See README section
# "Getting Features.pkl" for how to obtain or regenerate it.
Features/Features.pkl

# --- Dataset: licensed, must be requested from the authors ---------------
DATASET/**/*.mp4
DATASET/**/*.avi

# --- Python / editor noise ----------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.idea/
.vscode/
Thumbs.db
desktop.ini

# --- Local backups from document edits -----------------------------------
*.bak-*
FFPP/cache/
FFPP/results/

# Third-party reference implementations, cloned for comparison.
# Commit hashes are recorded in Optimized/oof_*.json.
External/
.ipynb_checkpoints/

# TLS-proxy CA bundle, machine-specific
Optimized/cache/win-ca-bundle.pem
# extracted FF++ metadata (regenerate with Optimized/extract_ffpp.py)
DATASET/FFPP_C23/
# Face crops built by Optimized/ffpp_prepare.py: ~64,000 JPEGs, and the ~2 GB
# archive of them staged for Google Drive. Both are regenerable build output,
# and the archive is far past GitHub's hard 100 MB per-file limit.
DATASET/ffpp_frames/
DATASET/ffpp_frames.zip
DATASET/ffpp_frames.zip.tmp
